form.pure-form.signUpForm(method="post" action="/signup")
    fieldset
        h3 Sign Up
        div.signup-form-container
            div
                div
                    //- label First Name
                    input(type="text" name='firstName' placeholder='First Name')
                div
                    //- label Last Name
                    input(type="text" name='lastName' placeholder='Last Name')
                div
                    //- label Username
                    input(type="text" name='userName' placeholder='User Name')
                div
                    button.pure-button.pure-button-primary(type="submit") Create Account
                    input(type="hidden" name='_csrf' value=token)
            div
                div
                    //- label Email
                    input(type="email" name='email' placeholder='Email')
                div
                    //- label Password
                    input(type="password" name='password' placeholder='Password')
                div
                    //- label Confirm Password
                    input(type="password" name='confirmPassword' placeholder=' Confirm Password')

//- include utils.pug
//-   +validationErrorSummary(errors)
//-   form(action='/user/register' method='post')
//-     input(type='hidden' name='_csrf' value=csrfToken)
//-     +field('First Name', 'firstName', user.firstName)
//-     +field('Last Name', 'lastName', user.lastName)
//-     +field('Username', 'userName', user.userName)
//-     +field('Email Address', 'email', user.email)
//-     +field('Password', 'password', user.password, 'password')
//-     +field('Confirm Password', 'confirmPassword', '', 'password')
//-     div(class='py-4')
//-       button(type='submit' class='btn btn-primary') Register
//-       a(href='/' class='btn btn-warning ml-2') Cancel
